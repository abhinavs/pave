fastapi>=0.115
uvicorn[standard]>=0.30
gunicorn>=22
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
# aiosqlite backs USE_SQLITE, a documented runtime option, so it ships in prod
# too (not just dev) or a sqlite install would hit ModuleNotFoundError.
aiosqlite>=0.20
alembic>=1.13
# pydantic is imported directly across app/, so it is a direct runtime dep,
# not merely transitive via pydantic-settings.
pydantic>=2.0
pydantic-settings>=2.0
authlib>=1.3
itsdangerous>=2.1
bcrypt>=4.0
python-multipart>=0.0.9
jinja2>=3.1
email-validator>=2.1
python-frontmatter>=1.0
markdown-it-py>=3.0
pygments>=2.17
sse-starlette>=2.0
slowapi>=0.1
starlette-csrf>=3.0
structlog>=24.0
prometheus-client>=0.20
Pillow>=10.0
httpx>=0.27
typer>=0.12
soniq>=0.0.3
